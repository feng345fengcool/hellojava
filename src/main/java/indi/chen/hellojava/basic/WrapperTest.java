package indi.chen.hellojava.basic;

public class WrapperTest {

    // 一、构造函数的参数有两种：①原始类型 ②字符串类型

    // 二、每个包装类型都有一个获取原始类型的成员函数如byteValue() floatValue() doubleValue() intValue() longValue() shortValue()

    // 三、字符串类型转化为包装类型可以通过valueOf方式和decode方式（Byte Short Integer Long）

    // 四、字符串类型转化为包装类的原始类型可以通过parse方式

    /**
     *
     * 1. Character
     * 		static boolean isLetter(char);
     * 		static boolean isLetterOrDigit(char);
     * 		static boolean isLowerCase(char);
     * 		static boolean isUpperCase(char);
     * 		static boolean isSpaceChar(char);
     * 		static boolean isWhitespace(char);
     *
     * 		static char reverseBytes(char);
     * 		static char toLowerCase(char);
     * 		static char toUpperCase(char);
     * 2. Short
     * 		static short reverseBytes(short);
     * 3. Integer Long
     * 		static int highestOneBit();
     * 		static int lowestOneBit();
     *
     * 		static int reverse(int);
     * 		static int reverseByte(int);
     *
     * 		static String toBinaryString(int);
     * 		static String toHexString(int);
     * 		static String toOctalString(int);
     * 4. Float Double
     * 		static String toHexString(float);
     *
     */

}
